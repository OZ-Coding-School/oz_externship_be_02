name: Code Quality Checks

on:
  pull_request:
    branches:
      - main
      - develop
  # 워크플로우를 수동으로 실행할 수 있도록 workflow_dispatch 추가
  workflow_dispatch:

permissions:
  contents: read
  actions: write
  pull-requests: write

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ startsWith(github.ref, 'refs/pull/') }}

env:
  PYTHON_VERSION: "3.12"

jobs:
  code-quality:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Cache Poetry
        uses: actions/cache@v4
        with:
          key: poetry-${{ runner.os }}-${{ env.PYTHON_VERSION }}-${{ hashFiles('poetry.lock') }}
          path: ~/.local/

      - name: Install Poetry
        run: curl -sSL https://install.python-poetry.org | python3 -

      - name: Add Poetry to PATH
        run: echo "$HOME/.local/bin" >> $GITHUB_PATH

      - name: Cache dependencies
        uses: actions/cache@v4
        with:
          key: deps-${{ runner.os }}-${{ env.PYTHON_VERSION }}-${{ hashFiles('poetry.lock') }}
          path: /home/runner/.cache/pypoetry/virtualenvs/

      - name: Install dependencies
        run: poetry install --no-root

      - name: Setup environment
        run: |
          mkdir -p envs
          echo "${{ secrets.DJANGO_ENVS }}" > envs/.local.env
          echo "DJANGO_SETTINGS_MODULE=config.settings.local" >> $GITHUB_ENV

      - name: Run code quality checks (parallel & fail-fast)
        shell: bash
        run: |
          set -euo pipefail
          pids=()
          poetry run isort . --check --diff & pids+=($!)
          poetry run black . --check & pids+=($!)
          poetry run mypy . & pids+=($!)
          status=0
          for pid in "${pids[@]}"; do
            if ! wait "$pid"; then
              status=1
            fi
          done
          exit $status

  discover-apps:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    outputs:
      matrix: ${{ steps.find-apps.outputs.matrix }}
      count: ${{ steps.find-apps.outputs.count }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install jq
        run: |
          sudo apt-get update
          sudo apt-get install -y jq

      - name: Find apps with tests
        id: find-apps
        shell: bash
        run: |
          set -euo pipefail
          # 'apps' 디렉터리 내의 모든 하위 디렉터리를 찾음
          apps=$(find apps -mindepth 1 -maxdepth 1 -type d -exec basename {} \; 2>/dev/null || echo "")
          if [ -z "$apps" ]; then
            echo "No apps found"
            echo "matrix={\"app\":[]}" >> $GITHUB_OUTPUT
            echo "count=0" >> $GITHUB_OUTPUT
          else
            echo "Found apps: $apps"
            app_list=$(echo "$apps" | jq -R . | jq -s . | jq -c .)
            matrix_json="{\"app\":$app_list}"
            echo "matrix=$matrix_json" >> $GITHUB_OUTPUT
            echo "count=$(echo "$apps" | wc -w)" >> $GITHUB_OUTPUT
            echo "Generated matrix: $matrix_json"
          fi

  test:
    runs-on: ubuntu-latest
    needs: discover-apps
    if: needs.discover-apps.outputs.count != '0'
    timeout-minutes: 20
    strategy:
      fail-fast: false
      max-parallel: 10
      matrix: ${{ fromJson(needs.discover-apps.outputs.matrix) }}
    services:
      db:
        image: postgres:14
        ports: ['5432:5432']
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: pw1234
          POSTGRES_DB: ozcoding_externship
          TZ: Asia/Seoul
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
      redis:
        image: redis:latest
        ports: ['6379:6379']
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 5s
          --health-timeout 3s
          --health-retries 5
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Cache Poetry
        uses: actions/cache@v4
        with:
          key: poetry-${{ runner.os }}-${{ env.PYTHON_VERSION }}-${{ hashFiles('poetry.lock') }}
          path: ~/.local/

      - name: Install Poetry
        run: curl -sSL https://install.python-poetry.org | python3 -

      - name: Add Poetry to PATH
        run: echo "$HOME/.local/bin" >> $GITHUB_PATH

      - name: Cache dependencies
        uses: actions/cache@v4
        with:
          key: deps-${{ runner.os }}-${{ env.PYTHON_VERSION }}-${{ hashFiles('poetry.lock') }}
          path: /home/runner/.cache/pypoetry/virtualenvs/

      - name: Install dependencies
        run: poetry install --no-root

      - name: Setup environment
        run: |
          mkdir -p envs
          echo "${{ secrets.DJANGO_ENVS }}" > envs/.local.env
          echo "DJANGO_SETTINGS_MODULE=config.settings.local" >> $GITHUB_ENV

      - name: Wait for services
        run: |
          timeout 30 bash -c 'until nc -z localhost 5432; do sleep 1; done'
          timeout 30 bash -c 'until nc -z localhost 6379; do sleep 1; done'

      - name: Run migrations
        run: poetry run python manage.py migrate

      - name: Run tests for ${{ matrix.app }}
        shell: bash
        run: |
          set -eo pipefail
          app_name="${{ matrix.app }}"
          app_path="apps/$app_name"
          echo "🚀 Testing $app_name..."

          # 앱 디렉터리 내에 테스트 파일 및 테스트 함수가 있는지 확인
          test_files=$(find "$app_path" -type f -name "test*.py")
          if [ -n "$test_files" ] && grep -q -r "def test_" $app_path; then
            echo "✅ Running tests for $app_name"
            poetry run coverage run --parallel-mode --source="$app_path" manage.py test "apps.$app_name" --verbosity=1
          else
            echo "⚠️ No test functions found in $app_name. Skipping coverage generation."
          fi

      - name: Upload coverage data for ${{ matrix.app }}
        # 'always()'를 사용하여 테스트 실패 시에도 커버리지 데이터 업로드
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: coverage-data-${{ matrix.app }}
          path: .coverage.*
          include-hidden-files: true # 숨김 파일도 볼 수 있도록 설정 변경
          if-no-files-found: ignore # 테스트가 없는 앱의 경우 무시
          retention-days: 3

  finalize:
    runs-on: ubuntu-latest
    needs: [code-quality, test]
    if: always()
    timeout-minutes: 5
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install coverage
        run: pip install coverage

      - name: Download all coverage artifacts
        uses: actions/download-artifact@v4
        with:
          # 'coverage-data-' 패턴으로 시작하는 모든 아티팩트를 다운로드
          pattern: coverage-data-*
          path: coverage-reports
          merge-multiple: true

      - name: Generate combined coverage report
        id: combined-coverage
        shell: bash
        run: |
          set -euo pipefail
          echo "📥 Downloaded artifacts contents into 'coverage-reports':"
          ls -laR coverage-reports/ || echo "No coverage-reports directory found."

          # coverage combine 명령어에 데이터 파일이 있는 디렉터리를 직접 지정
            echo "📊 Combining coverage data from 'coverage-reports' directory..."
            coverage combine --keep coverage-reports


          if [ -f ".coverage" ]; then
            echo "✅ Coverage data combined successfully."
            ls -la .coverage
            
            echo "📊 Generating Combined Coverage Report:"
            coverage report -m
            coverage xml -o coverage.xml
            echo "✅ Combined coverage report generated: coverage.xml"
            echo "combined=true" >> $GITHUB_OUTPUT
          else
            echo "⚠️ Failed to combine coverage data. No '.coverage' file was created."
            echo "Checking for raw data files in coverage-reports..."
            find coverage-reports -type f -name ".coverage.*" -print || echo "No raw .coverage.* files found."
            echo "combined=false" >> $GITHUB_OUTPUT
          fi

      - name: Upload combined coverage artifact
        if: steps.combined-coverage.outputs.combined == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: combined-coverage-report
          path: |
            coverage.xml
            .coverage
          retention-days: 30

      - name: Check overall CI status
        run: |
          if [[ "${{ needs.code-quality.result }}" == "success" && 
                ("${{ needs.test.result }}" == "success" || "${{ needs.test.result }}" == "skipped") ]]; then
            echo "✅ All CI checks passed!"
          else
            echo "❌ CI failed - Code Quality: ${{ needs.code-quality.result }}, Tests: ${{ needs.test.result }}"
            exit 1
          fi

